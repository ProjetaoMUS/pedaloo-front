import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flexDirection: 'row',
      alignItems: 'center',
      borderWidth: 1,
      borderColor: '#D3D3D3',
      borderRadius: 8,
      paddingHorizontal: 10,
      marginBottom: 10,
      borderBottomWidth: 2,
    },
    input: {
      flex: 1,
      height: 40,
    },
    icon: {
      marginLeft: 10,
    },
  });